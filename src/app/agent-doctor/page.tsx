import Link from 'next/link'; import { pageMetadata } from '@/lib/pageMetadata'; import { publicRoute } from '@/lib/publicRoutes'; import { AGENT_DOCTOR } from '@/lib/github/releases';
const route = publicRoute('/agent-doctor'); export const metadata = pageMetadata(route);
export default function Page(){ return <main className="feature-page shell"><header className="feature-page__header"><p className="eyebrow">{route.eyebrow}</p><h1>{route.heading}</h1><p>{route.description}</p></header><section className="download-hero"><div><p className="eyebrow">OFFICIAL NEXOTOKEN RELEASE</p><h2>Local evidence for coding-agent work that cannot afford to loop.</h2><p>Agent Doctor keeps bounded task evidence, runtime guidance and confirmed context on your machine. It is a separate local tool: the Radar web tester does not receive its local task history.</p><div className="hero__actions"><a className="button" href={AGENT_DOCTOR.latestRelease}>Download latest release</a><a className="button button--secondary" href={AGENT_DOCTOR.repository}>Inspect source on GitHub</a></div></div><div className="download-console"><span>AGENT DOCTOR / LOCAL</span><strong>Evidence before assertions.</strong><code>context · runtime · validation</code><i>Official repository: 18534516725/Agent-Doctor</i></div></section><section className="platform-grid"><article><span>01</span><h2>macOS</h2><p>Use the release artifact matching Apple Silicon or Intel. Verify the published checksum before installation.</p></article><article><span>02</span><h2>Windows</h2><p>Choose the Windows artifact and compare its SHA-256 value with the release manifest.</p></article><article><span>03</span><h2>Linux</h2><p>Select the artifact for your architecture. Keep local evidence files outside public repositories.</p></article></section><aside className="integrity-note"><div><p className="eyebrow">SUPPLY-CHAIN CHECK</p><h2>Download only from the official repository.</h2></div><p>Radar never mirrors installers. Verify the exact filename and SHA-256 checksum, and report discrepancies through the <a className="text-link" href={AGENT_DOCTOR.issues}>official issue tracker</a>.</p></aside><Link className="text-link" href="/doctor">Need to test an API endpoint instead? Open the private web Doctor →</Link></main>; }
