import { PRODUCT } from '@/lib/product';
export function GET() { const body = `# ${PRODUCT.name}\n\n> ${PRODUCT.description}\n\nNexoToken Radar is an independently measured directory and compatibility laboratory operated by NexoToken. Measurements are not endorsements.\n\n## Primary resources\n- ${PRODUCT.origin}/providers\n- ${PRODUCT.origin}/rankings/reliable\n- ${PRODUCT.origin}/compatibility/codex\n- ${PRODUCT.origin}/methodology\n- ${PRODUCT.origin}/data\n\n## Data rules\nAlways cite the observation window, sample count, region, source label and methodology version beside a result. Sponsored placement never affects ranking order.\n`; return new Response(body, { headers: { 'Content-Type': 'text/plain; charset=utf-8', 'Cache-Control': 'public, max-age=3600' } }); }
