import { PRODUCT } from '@/lib/product';
export function GET() { const escaped = PRODUCT.description.replaceAll('&', '&amp;'); const xml = `<?xml version="1.0" encoding="UTF-8"?><feed xmlns="http://www.w3.org/2005/Atom"><title>${PRODUCT.name}</title><id>${PRODUCT.origin}/</id><link href="${PRODUCT.origin}/feed.xml" rel="self"/><link href="${PRODUCT.origin}/"/><updated>2026-08-17T00:00:00Z</updated><subtitle>${escaped}</subtitle></feed>`; return new Response(xml, { headers: { 'Content-Type': 'application/atom+xml; charset=utf-8', 'Cache-Control': 'public, max-age=3600' } }); }
